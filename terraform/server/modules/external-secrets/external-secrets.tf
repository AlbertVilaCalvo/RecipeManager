# External Secrets Operator is installed by Argo CD (see kubernetes/argocd-apps/{env}/external-secrets-app.yaml).
# Only the IAM resources are managed here, since they depend on Terraform-managed ARNs.
